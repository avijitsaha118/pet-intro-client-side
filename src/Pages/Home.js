import React from 'react';
import SelectedInfo from './SelectedInfo';

const Home = () => {
    return (
        <div>
            <h1>This is home.</h1>
            <SelectedInfo></SelectedInfo>
        </div>
    );
};

export default Home;